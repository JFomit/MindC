#include "units.mindch"
#include "math.mindch"

void main() {
    number iterations;
    bind_unit();

    while (true) {
        move_unit(50 * cos(iterations) + 150, 50 * sin(iterations) + 150);
        iterations = iterations + 1;
    }
}
